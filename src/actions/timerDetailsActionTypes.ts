const actionGroupName = 'TimerDetailsActions';

export const CHANGE_NAME = `${actionGroupName}.ChangeName`;
export const EDIT_MODE = `${actionGroupName}.EditMode`;
export const SET_DURATION = `${actionGroupName}.SetDuration`;
export const SAVE_TIMER = `${actionGroupName}.SaveTimer`;
